# Assets

Reserved for static assets (logos, demo photos/videos) used in the pitch
deck or README. None are bundled in this snapshot — the pitch deck
(`submission/short_pitch.pptx`) uses vector shapes/text only, no raster
images, to keep the repository lightweight.
